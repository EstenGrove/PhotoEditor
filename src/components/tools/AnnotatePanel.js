import React from "react";
import styles from "../../css/tools/AnnotatePanel.module.scss";
import { PropTypes } from "prop-types";

const AnnotatePanel = () => {
	return (
		<div className={styles.AnnotatePanel}>
			{/*  */}
			{/*  */}
			{/*  */}
		</div>
	);
};

export default AnnotatePanel;

AnnotatePanel.defaultProps = {};

AnnotatePanel.propTypes = {};
